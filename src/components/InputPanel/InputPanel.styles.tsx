import { styled } from "styled-components";

const InputPanelContainer = styled.section`
	display: flex;
	flex-wrap: wrap;
	justify-content: center;
	gap: 10px;
	align-items: flex-end;
`

export default InputPanelContainer
